# =============================================================================
# SERVIDOR CON DOCKER — Turnos Dispensario
# IP: 192.168.20.26
# =============================================================================
#
# Modo A — TODO EN DOCKER (recomendado):
#   deploy\windows\1-instalar-docker.bat
#   deploy\windows\2-iniciar-docker.bat
#
# Modo B — Solo PostgreSQL en Docker + app con Node en el servidor:
#   Use esta DATABASE_URL (puerto 5544 solo en localhost)
#
# NO necesita pgAdmin ni crear usuario manualmente (Docker lo crea).

DATABASE_URL="postgresql://turnos:TdCencoic2026Disp@127.0.0.1:5544/turnos_dispensario?schema=public"

JWT_SECRET="cencoic-turnos-jwt-2026-k8mP2xQ9vL4nR7wZ6sH3fA1bN5jD0eU"
JWT_EXPIRES_IN="12h"

PORT=8741
HOST=0.0.0.0
PUBLIC_SERVER_IP=192.168.20.26
CORS_ORIGIN="*"
NODE_ENV=production
