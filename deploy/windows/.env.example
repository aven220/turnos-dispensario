# Copie este archivo a backend\.env antes de iniciar
# Puertos poco usados para no chocar con otras aplicaciones

DATABASE_URL="postgresql://turnos:turnos123@127.0.0.1:5544/turnos_dispensario?schema=public"
JWT_SECRET="cambiar-por-un-texto-largo-y-seguro-en-produccion"
JWT_EXPIRES_IN="12h"

# Un solo puerto para toda la aplicacion (web + API + tiempo real)
PORT=8741
CORS_ORIGIN="http://localhost:8741"

# 127.0.0.1 = solo este equipo | 0.0.0.0 = otros PCs de la red local
HOST=0.0.0.0
