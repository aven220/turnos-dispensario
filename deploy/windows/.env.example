# === BASE DE DATOS LOCAL (recomendado, gratis, sin suscripciones) ===
# Opcion A: Docker  -> el script 1-instalar.bat lo configura automaticamente
# Opcion B: PostgreSQL instalado en Windows (ver README.md)
#
# URL por defecto para Docker local (puerto 5544, solo este equipo):

DATABASE_URL="postgresql://turnos:TdCencoic2026Disp@127.0.0.1:5544/turnos_dispensario?schema=public"

JWT_SECRET="cencoic-turnos-jwt-2026-k8mP2xQ9vL4nR7wZ6sH3fA1bN5jD0eU"
JWT_EXPIRES_IN="12h"

# Un solo puerto para toda la aplicacion (web + API + tiempo real)
PORT=8741
CORS_ORIGIN="http://localhost:8741"

# 127.0.0.1 = solo este equipo | 0.0.0.0 = otros PCs de la red local
HOST=0.0.0.0
